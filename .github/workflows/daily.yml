- name: Commit & Push
        run: |
          git config --global user.name "Shadow-Bot"
          git config --global user.email "bot@shadow.com"
          git add .
          # This line only commits if there are actual changes
          git diff --quiet && git diff --staged --quiet || (git commit -m "New observation" && git push)
